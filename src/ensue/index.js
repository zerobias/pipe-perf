const { es5ReduxNF, reduxNoFilter, reduxFilter } = require('./redux')
const { v2Bab, v2Orig } = require('./ensue-old')
const { enDev, eFlat, eSlice } = require('./ensue-tests')

module.exports = {
  es5ReduxNF, reduxNoFilter, reduxFilter,
  v2Bab, v2Orig,
  enDev, eFlat, eSlice
}